import { MobileRoutes } from '@/platforms/mobile/routes';
import Layout from '@/platforms/unified/components/Layout';

export default function Mobile() {
  return (
    <Layout>
      <MobileRoutes />
    </Layout>
  );
}

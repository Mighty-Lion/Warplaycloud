import styled from '@emotion/styled';

export const NavbarButton = styled.button`
	position: relative;
  border: none;
  background: inherit;
  font-family: Roboto;
  font-size: 16px;
  font-weight: 300;
  color: #ffffff;
	&:hover {
    color: gray;
  }
`;

import styled from '@emotion/styled';

export const NavbarButton = styled.button`
  position: relative;
	padding: 0;
  border: none;
  background: inherit;
  font-family: Roboto;
  font-size: 16px;
  font-weight: 300;
	white-space: nowrap;
  color: #ffffff;
  &:hover {
    color: gray;
  }
`;
